server:
  port: 8080
spring:
  application:
    name: api-gateway
services:
  ingestion:
    url: ${INGESTION_URL:http://localhost:8081}
  transformer:
    url: ${TRANSFORMER_URL:http://localhost:8082}
  analysis:
    url: ${ANALYSIS_URL:http://localhost:8083}
  knowledge-store:
    url: ${KNOWLEDGE_STORE_URL:http://localhost:8084}
  similarity:
    url: ${SIMILARITY_URL:http://localhost:8085}
  simulator:
    url: ${SIMULATOR_URL:http://localhost:8086}
